[build]
  command = "apt-get update -qq && apt-get install -y gdal-bin libgdal-dev && pip install -r requirements.txt && python backend/manage.py collectstatic --noinput"
  publish = "staticfiles"

[build.environment]
  PYTHON_VERSION = "3.11"
  NETLIFY = "true"
